# -------------------------------
# Core Web Framework
# -------------------------------
flask                 # Backend API to handle RAG logic
flask-cors            # Allows frontend (Streamlit) to call Flask API
gunicorn              # Production WSGI server (needed for deployment)

# -------------------------------
# Streamlit Frontend
# -------------------------------
streamlit             # UI framework for building the chatbot interface
python-dotenv         # Loads environment variables from .env / secrets.toml

# -------------------------------
# LangChain (RAG Orchestration)
# -------------------------------
langchain             # Core LangChain abstractions (chains, prompts, LLMs)
langchain-community   # Community integrations (retrievers, loaders, utils)
langchain-google-genai # Google Gemini + Embeddings support
langchain-groq        # Groq LLaMA 3.1/3.3 fast inference support

# -------------------------------
# Vector Database (Embeddings)
# -------------------------------
chromadb              # Stores embeddings & performs semantic retrieval

# -------------------------------
# LLM SDKs (Direct API calls)
# -------------------------------
google-generativeai   # Official Google Gemini SDK

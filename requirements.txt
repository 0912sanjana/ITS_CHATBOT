# =====================================================
# Core Web Framework (Backend API)
# =====================================================
flask                     # Backend API (if you want Flask mode)
flask-cors                # Enable CORS for Streamlit frontend
gunicorn                  # Production WSGI server (for deployment)

# =====================================================
# Streamlit Frontend (Chat UI)
# =====================================================
streamlit                 # Web UI for ITS RAG Chatbot
python-dotenv             # Load .env or Streamlit Secrets

# =====================================================
# LangChain (RAG Core)
# =====================================================
langchain-core<0.3.0      # Avoids pydantic_v1 errors
langchain-community        # All loaders, retrievers, utilities
langchain-groq             # Groq LLaMA 3.1 / 3.3 high-speed LLM support

# =====================================================
# Google Gemini SDK (Official) — SAFE
# (Replaces langchain-google-genai which breaks on Cloud)
# =====================================================
google-generativeai        # Official Gemini API client

# =====================================================
# Embeddings + Vector Database
# =====================================================
sentence-transformers      # Local embedding model (MiniLM)
chromadb                   # Vector DB for semantic search
faiss-cpu                  # Optional FAISS support (local testing)

# =====================================================
# Utilities
# =====================================================
numpy                      # Math operations
pandas                     # Data handling
requests                   # API calls
